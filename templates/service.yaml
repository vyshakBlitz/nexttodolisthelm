apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexthelm.fullname" . }}
  labels:
    {{- include "nexthelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexthelm.selectorLabels" . | nindent 4 }}
