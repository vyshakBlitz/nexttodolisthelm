apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nexthelm.fullname" . }}-test-connection"
  labels:
    {{- include "nexthelm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nexthelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
